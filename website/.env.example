# Server Configuration
PORT=3000
NODE_ENV=development

# Session Secret (generate a random string for production)
SESSION_SECRET=your-secret-key-here

# Firebase Configuration
# For public database rules, only the database URL is needed
# For secured rules, add service account credentials
FIREBASE_DATABASE_URL=https://sharecircle-f335e-default-rtdb.asia-southeast1.firebasedatabase.app/
